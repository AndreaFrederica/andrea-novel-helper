# 测试文档 Hover 功能

## TypeScript 代码示例

```typescript
const x: number = 1;
x.toFixed(2);

function greet(name: string): string {
    return `Hello, ${name}!`;
}

greet("World");
```

## Markdown 链接测试

这是一个链接测试：[Google](https://www.google.com)

## 引用测试

> 这是一个引用块
> 包含多行内容

## 代码块测试

```javascript
const array = [1, 2, 3];
array.map(x => x * 2);
```

## 表格测试

| 列1 | 列2 | 列3 |
|-----|-----|-----|
| A   | B   | C   |
| D   | E   | F   |
# 增强的角色树视图

## 概述

角色树视图现在支持显示详细的优先级信息和角色属性，让你能够清楚地了解每个角色的优先级构成。

## 新增功能

### 1. 优先级信息展示

在角色节点下，优先显示以下优先级相关信息：

- **⚡ 总优先级**：计算后的最终优先级值
- **📄 文件优先级**：文件中使用 `_priority` 字段定义的优先级
- **📊 优先级构成**：显示优先级的详细来源

### 2. 优化的属性展示

- 使用图标和标签提高可读性
- 基础属性（名称、UUID、类型）优先显示
- 其他属性按字母顺序排列
- 自动过滤内部属性（以 `_` 开头的）

### 3. 视觉改进

- 📝 名称
- 🆔 UUID
- 🏷️ 类型
- 📝 描述
- 🎨 颜色
- 🏠 从属
- 📛️ 别名
- 🔧 修复
- ⚡ 优先级
- 📁 包路径
- 📄 源文件
- 🖼️ 图标

## 实际示例

### 示例1：具有高优先级的角色

```
📜 主要角色
├── 🏷️ 主角
│   └── 张三
│       ├── ⚡ 总优先级: 2000
│       ├── 📄 文件优先级: 1000
│       ├── 📊 优先级构成: 来源: fileName
│       │   优先级构成详情:
│       │       • 文件名优先级: +1000
│       ├── 🆔 UUID: char-001
│       ├── 🏷️ 类型: 主角
│       ├── 🎨 颜色: #FF0000
│       ├── 📛️ 别名: 小张, 阿三
│       └── 📄 源文件: __zhangsan.json5
```

### 示例2：合并角色的优先级构成

```
📜 未分组
└── 🏷️ 角色
    └── 张三
        ├── ⚡ 总优先级: 1100
        ├── 📄 文件优先级: 500
        ├── 📊 优先级构成: 来源: fileDefined, fileName
        │   优先级构成详情:
        │       • 文件定义: +500
        │       • 文件名优先级: +600
        ├── 🆔 UUID: char-001
        ├── 🏷️ 类型: 角色
        ├── 🎨 颜色: #FF0000
        ├── 🏠 从属: 主角团
        ├── 📛️ 别名: 小张, 阿三
        ├── 🔧 修复: 张小三子, 小三
        └── 📄 源文件: novel-helper/characters/zhangsan.json5
```

## 优先级构成说明

### 优先级来源

- **文件定义**：在文件中使用 `_priority` 字段定义
- **位置优先级**：外部文件 vs 内部文件的优先级差异
- **文件名优先级**：基于文件名前缀（`__`、`!!`等）
- **文件类型优先级**：基于文件扩展名（`.ojson5`、`.json5`、`.md`、`.txt`）
- **默认优先级**：配置的默认值

### 优先级计算

```
总优先级 = 文件定义优先级
          + 位置优先级（如果启用）
          + 文件名优先级（如果启用）
          + 文件类型优先级（如果启用）
          + 默认优先级
```

## 使用建议

1. **调试优先级**：展开角色节点查看优先级构成，了解为什么某个角色具有更高的优先级

2. **快速识别**：通过图标快速识别优先级相关的节点

3. **问题排查**：当角色合并不如预期时，查看优先级构成，确定问题所在

4. **优化配置**：根据优先级构成调整配置，优化角色合并策略

## 配置选项

在 VSCode 设置中，可以通过以下选项控制显示：

```json
{
  "AndreaNovelHelper": {
    "roles": {
      "details": {
        "enableRoleExpansion": true,
        "alwaysExpandable": true,
        "wrapColumn": 80
      }
    }
  }
}
```

## 注意事项

- 优先级信息只在角色加载时计算并保存
- 合并角色的优先级使用基础角色的值
- 内部属性（以 `_` 开头）不会显示在详情中
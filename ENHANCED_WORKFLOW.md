# 增强版随机角色生成工作流程

## 功能概述

本文档描述了 Andrea Novel Helper 插件的增强版随机角色生成工作流程，该工作流程集成了翻译功能、别名支持和光标插入功能。

## 工作流程步骤

### 6步式完整工作流程

1. **步骤 1**: 选择文化背景
   - **亚洲语言**：中文（简体/繁体）、日语、韩语、泰语、越南语、印尼语、印地语、阿拉伯语、希伯来语
   - **欧洲语言**：
     - **英语**：美国、英国、澳大利亚、加拿大、爱尔兰、印度、南非
     - **法语**：法国、加拿大、比利时、瑞士、卢森堡、塞内加尔
     * **德语**：德国、奥地利、瑞士
     * **西班牙语**：西班牙、墨西哥
     * **意大利语**：意大利
     * **葡萄牙语**：巴西、葡萄牙
     * **俄语**：俄罗斯、乌克兰
     * **北欧语言**：瑞典、挪威、丹麦、芬兰、冰岛
     * **荷兰语**：荷兰、比利时
     * **东欧语言**：波兰、捷克、匈牙利、克罗地亚、罗马尼亚、斯洛伐克
     * **南欧语言**：希腊、土耳其
   - **奇幻风格**：支持多种奇幻命名风格
   - 每种文化支持不同的风格（现代、经典、奇幻、科幻、历史等）

2. **步骤 2**: 选择性别
   - 男性、女性、中性、随机

3. **步骤 3**: 选择风格
   - 根据文化背景提供相应的风格选项

4. **步骤 4**: 选择翻译选项（新增）
   - 询问是否为生成的名字添加本地化翻译别名
   - 可选择仅使用原名或生成翻译

5. **步骤 5**: 选择目标语言（如果需要翻译）
   - 支持多种目标语言：中文、英文、日语、德语、法语、韩语、繁体中文、拉丁语、希腊语
   - 使用现有的 LLM 翻译系统进行批量翻译

6. **步骤 6**: 选择最终名字并选择处理方式
   - 显示生成的名字列表，第一项为"🎲 重新生成"选项
   - 显示原名和翻译名（如果生成）
   - 可选择创建角色文件或直接插入到光标位置

## 主要增强功能

### 1. 翻译功能集成
- 使用现有的 `translateTextWithClientLLM` 翻译系统
- 批量为所有生成的名字创建翻译
- 显示翻译状态指示器
- 支持翻译错误处理

### 2. 智能别名生成
- 自动将翻译名添加到角色的 `aliases` 数组中
- 仅在翻译名存在时才添加别名
- 保留原名作为主要名称

### 3. 灵活的插入选项
- **直接插入模式**：在选择名字后可直接插入到光标位置
- **角色文件模式**：创建完整的角色文件并可选择后续插入
- **多种插入版本**：原名、翻译名、双语版本（原名 (翻译名)）

### 4. 重新生成功能
- 在名字选择列表中提供"🎲 重新生成"作为第一项
- **智能状态保持**：重新生成时保持之前的翻译选择和目标语言，避免重复询问
- 快速重新生成新的名字选项
- 一次设置，多次使用的优化体验

### 5. 用户友好的界面
- 清晰的步骤指示（步骤 x/6）
- 详细的名字信息显示（文化、性别、风格、起源、翻译）
- 智能的选项禁用（例如没有翻译时禁用翻译相关选项）

## 技术实现

### 策略系统架构
- **自定义策略**：中文、日语、奇幻风格使用专门的策略类
- **通用 Faker 策略**：其他所有文化使用统一的 `FakerLocaleStrategy`
- **回退策略**：当所有策略都失败时的最后保障

### Faker 本地化支持
- 利用 faker 库的 70+ 种本地化支持
- 自动映射文化代码到 faker 本地化模块
- 智能格式化：不同文化的名字格式（空格使用等）

### 名字格式规则
- **中文/日语**：姓氏 + 名字（无空格），如"李明"、"佐藤誠"
- **欧洲语言**：名字 + 姓氏（有空格），如"John Smith"
- **其他语言**：根据当地习惯自动格式化

### 关键文件修改
- `src/commands/nameGeneratorCommands.ts`: 主要工作流程实现
- `src/typo/typoClientLLM.ts`: 导出 `getClientCfg` 函数
- `src/typo/typoService.ts`: 导出翻译状态函数

### 核心函数
1. `showCharacterNameSelection()`: 增强版名字选择界面
2. `createCharacterName()`: 支持翻译别名和光标插入的角色创建

### 翻译集成
- 复用现有的 `translateTextWithClientLLM` 函数
- 使用 `showTranslationStatus` 和 `hideTranslationStatus` 显示进度
- 批量翻译所有候选名字

## 角色文件格式

生成的角色文件包含以下字段：
```json5
{
  name: "原名",
  aliases: ["翻译名"], // 如果生成了翻译
  type: "主角",
  uuid: "唯一标识符",
  gender: "性别",
  affiliation: "从属标签", // 可选
  description: "角色简介",
  color: "#颜色代码", // 可选
  origin: "Andrea Novel Helper - 随机生成"
}
```

**注意**: 不再保存 `generationOptions` 字段到角色文件中，保持文件简洁。

## 使用场景

### 1. 小说创作
- 快速生成多文化背景的角色名
- 为不同语言版本的准备角色别名
- 直接插入到创作文档中

### 2. 游戏设计
- 创建多样化的角色命名
- 支持本地化版本的别名
- 快速填充角色数据库

### 3. 国际化项目
- 生成多语言角色名
- 保持原名的同时提供本地化版本
- 便于不同语言版本的内容适配

## 配置要求

- 需要配置 LLM 翻译功能（`AndreaNovelHelper.typo.clientLLM.enabled: true`）
- 需要设置翻译目标语言列表（`AndreaNovelHelper.translate.targets`）
- 需要指定默认目标语言（`AndreaNovelHelper.translate.defaultTarget`）

## 权限和兼容性

- 兼容现有的角色管理系统
- 复用现有的文件选择器和角色文件格式
- 不影响现有的名字生成功能
- 支持所有现有的文化和风格选项